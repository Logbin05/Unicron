export enum InstitutionType {
  SHCOOL = "Shcool",
  COLLEGE = "College",
  UNIVERSITY = "University",
}

export enum InstRole {
  ADMIN = "Admin",
  TEACHER = "Teacher",
  STUDENT = "Student",
}

export enum SubscriptionStatus {
  ACTIVE = "Active",
  EXPIRED = "Expired",
  CANCELLED = "Cancelled",
}

export enum EnrollmentStatus {
  ENROLLED = "Enrolled",
  COMPLETED = "Completed",
  DROPPED = "Dropped",
  PENDING = "Pending",
}

export enum ContentType {
  TEXT = "text",
  VIDEO = "video",
  MP3 = "mp3",
}

export enum KindType {
  TEST = "test",
  CASE = "case",
  PROJECT = "project",
  TESTDAD = "Drag And Drop",
}

export enum StatusLesson {
  NOT_STARTED = "Not started",
  IN_PROGRESS = "In progress",
  COMPLETED = 'Completed',
}